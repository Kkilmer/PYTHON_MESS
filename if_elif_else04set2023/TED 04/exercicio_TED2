# Solicita ao usuário o número de maçãs compradas
numero_maca = int(input("Digite o número de maçãs compradas: "))

# Preço unitário das maçãs
preco_unitario = 1.0  # Preço por maçã quando compradas pelo menos 12
preco_unitario_menos_de_12 = 1.3  # Preço por maçã quando compradas menos de 12

# Calcula o custo total da compra
if numero_maca >= 12:
    custo_total = numero_maca * preco_unitario
else:
    custo_total = numero_maca * preco_unitario_menos_de_12

# Exibe o custo total da compra
print(f"O custo total da compra é de R$ {custo_total:.2f}")
